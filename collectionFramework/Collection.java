package collectionFramework;

public class Collection {
    public static void main(String[] args) {
        /*
         * :INTERFACE:
         * ------------
         * collection:-An object that represents a group of objects,known as its
         * elements.(root for all other collection types)
         * List:-ordered collection can contain duplicate elements
         * (eg.-arraylist,linkedlist,vector,stack)
         * Set:-ordered collection can't contain duplicate elements
         * Queue:-designed for holding elements prior to processing
         * Deque:-double ended queue that allows insertion and removal from both ends
         * Map:-represents a collection of key-value pair.
         * 
         */

    }

}
